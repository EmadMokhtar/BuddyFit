run:
  timeout: 5m
  skip-dirs:
    - vendor
    - node_modules
    - .git
  skip-files:
    - ".*\\.pb\\.go$"
  modules-download-mode: readonly

linters:
  enable:
    - gofmt
    - golint
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - misspell
    - gocyclo
    - dupl
    - goconst
    - goimports

linters-settings:
  govet:
    check-shadowing: true
  gocyclo:
    min-complexity: 15
  dupl:
    threshold: 100

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
        - errcheck
        - dupl
        - gosec
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
